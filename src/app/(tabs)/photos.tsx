import React from 'react';
import { ComingSoonScreen } from '../../components/ui';

export default function PhotoCollectionScreen() {
  return (
    <ComingSoonScreen
      icon="images"
      title="Photo Collection"
      description="Guest photo uploads via a shared QR code need file-storage hosting this app doesn't have yet. This screen will connect once that's set up."
    />
  );
}

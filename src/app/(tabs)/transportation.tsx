import React from 'react';
import { ComingSoonScreen } from '../../components/ui';

export default function TransportationScreen() {
  return (
    <ComingSoonScreen
      icon="bus"
      title="Transportation"
      description="Guest transport and car assignments are on the roadmap. For the groom's procession, use Baraat Tracker under Live Wedding today."
    />
  );
}
